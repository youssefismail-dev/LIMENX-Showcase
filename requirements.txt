# LIMENX — Engineering Showcase dependencies.
#
# Deliberately light: the heavy ML stack (scikit-learn, CatBoost, PyTorch,
# SHAP) belongs to the proprietary detection engine, which is NOT part of this
# repository. Everything here runs against the reference engine in `engine/`.

# --- REST API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
prometheus-client>=0.20

# --- data contracts used by the model interfaces / validation ---
numpy>=1.24
pandas>=2.0

# --- tests ---
pytest>=8.0
httpx>=0.27          # required by starlette's TestClient
